# Count Example

The count example launches a specified number of instances using a `count` parameter.

Running the example:

```
$ export AWS_ACCESS_KEY_ID="your-access-key"
$ export AWS_SECRET_ACCESS_KEY="your-secret-key"
$ terraform init
$ terraform apply
```

Destroying the example:

```
$ terraform destroy
```
